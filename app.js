const testData = require('./mock-data.js');
const { findMulitpleMin } = require('./helpers.js');
const mockData = require('./mock-data.js');


// prodji kroz niz
// nadji minimum

const minData = findMulitpleMin(mockData);

const checkLeftRight = ({value, index}, arr) => {
}


console.log(minData); 
